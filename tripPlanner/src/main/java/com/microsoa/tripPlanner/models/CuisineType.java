package com.microsoa.tripPlanner.models;

// This enum should be identical to rest.spring.provider_food.model.CuisineType
// if it's consuming the same JSON structure.
public enum CuisineType {
    ITALIAN,
    CHINESE,
    MEXICAN,
    INDIAN,
    AMERICAN,
    JAPANESE,
    FRENCH,
    // Add other cuisine types as needed
}